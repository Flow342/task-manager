import { FC } from "react";
import styles from "./SettingsPage.module.sass";

const SettingsPage: FC = () => {
    return <div className={styles.wrapper}>SettingsPage</div>;
};

export default SettingsPage;
